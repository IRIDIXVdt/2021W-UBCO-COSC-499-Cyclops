import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cyclopediapp.firebaseapp.com',
  appName: 'Cyclops',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
